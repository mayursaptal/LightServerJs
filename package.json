{
  "name": "lightserverjs",
  "version": "1.0.7",
  "description": "Lightweight Node.js framework for web applications.",
  "main": "dist/bundle.cjs.js",
  "module": "dist/bundle.js",
  "exports": {
    "require": "./dist/bundle.cjs.js",
    "import": "./dist/bundle.js"
  },
  "scripts": {
    "start": "node dist/bundle.cjs.js",
    "build": "mkdir -p dist && esbuild index.js --bundle --platform=node --minify --format=esm --outfile=dist/bundle.js && esbuild index.js --bundle --platform=node --minify --format=cjs --outfile=dist/bundle.cjs.js"
  },
  "author": "Mayur Saptal",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/mayursaptal/LightServerJs"
  },
  "type": "module",
  "engines": {
    "node": ">=14.0.0"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
